#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	ll x,y;
	cin >> x >> y;
	
	vector<int> h;
	for(ll i=0; i<x; i++) {
		ll z;
		cin >> z;
		h.push_back(z);
	}
	
	
	
	
	return 0;	
}
